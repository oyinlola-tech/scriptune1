/**
 * Commands owned by the http module.
 *
 * The http module currently exposes no commands; system state changes are
 * driven by the runtime lifecycle rather than by requests.
 *
 * @module modules/http/commands
 */
export {};
