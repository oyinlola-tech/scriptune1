# Public URL of the Scriptune API (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:4000
# Public URL of this web app, used for metadata and share links
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Port for `npm run dev` / `npm start` (defaults to 3000)
# WEB_PORT=3001
